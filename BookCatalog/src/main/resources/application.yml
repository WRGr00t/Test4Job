spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true
  datasource:
    url: jdbc:mysql://localhost:3306/book_catalog?serverTimezone=UTC
    username: {insertUser}
    password: {insertPass}